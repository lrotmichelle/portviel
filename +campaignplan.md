+campaign on clcik => open  campaign 
 pop up form on: ≤360px & ≤760 px - center the form - allow scroll max 
                ≥ 760 px exacly beneath the button max width 38%
    max width: ≤360px 80%
               ≤760 px 60%
               ≥760 px 40%

    campaign name : rules 
                - char ≤ 14
                - not start / end with space or periods
                - no special char
                - only letters prevent the rest
                - don't enter anything if its not a letter
                - prevent styles{paste from another text styling website}.. 
                - even on pasting designed text, plz convert it to my site font & styles
    
    description : rules 
                - char ≤ 325
                - not start with space or period
                - not start with space or period
                - no special char text only
                - only letters 
                - don't allow anything{emoji , pics ,} if its not a letter
                - prevent styles{paste from another text styling website}.. 
                - even on pasting designed text, plz convert it to my site font & styles
                - don't allow links links

category & nich filters:
                - atleast 1 filter should be selected from each 
                - on clcik add, if there is no selected filter - disable the add button
                - print a helper text that { at least 1 {group filter(skipped filter) }- must be selected}- change the input border color to red

add button :
               - they require a real selection and cannot add the empty placeholder.

on screen size > 360px 
category & nich filter : budget , minpayour & max payout :future 

budget , max , min:
                - only numbers must be entered
                - don't enter anything if its not a number
                - don't allow commers or periods, or char thats not a number
                - keep the min and max logic don't change it
 days remaining => should be centered with even spacing

 add a cancel button to be side by side of the publish button

 link with office with +campaign in the campaign page
  inside the budget
     - balance
          - check if user has a 1k on the account
          - check if the user has that allocated amount in the budget even when we subtract 1k as site charges
          for example if a user has 99k on account the budget should not exxceed 98k as a 1k is for the site charges.
          - collect that allocated budget then place it into the campaign manager wallet to the balance account immediately when the publish putton is pressed and the camapaign is posted

 
    office - add user account balance 
                   - calculate to show balances
                   - transactions
                   - payment methods - airtel money & mobile money or bank card

    on the manage page create a wallet to show balance,Due & settled ... add a calculator function too

    office & manage page sync on clcik publish

    cpm 0.01 max is 0.1%
    lets add some logic to the cpm, with the min to be % and max to be 0.1% of the budget, don't allow something smaller or bigger than that
    min payou 0.1% – 0.5%
    max payout 4% - 8%


    cpm ranges from 10% to 20% of the budget value.
    cpm the min cpm is 10% of the budget value  but the max is 20% of the budget value
    cpm value is ≥ 10% of the budget but ≤ 20% of the budget
helper text: cost per 1K {cpm value in k}
    
    min payout ranges from 0.1% to 0.5% 
    min payout value => formular{rage% * budget value * cpm value }....
 helper text: min {min payout value}

   max payout ranges from 4% to 8% 
    max payout value => formular{rage% * budget value * cpm value }....
 helper text: min {max payout value}

 rules: numbers only
        dont enter periods, commers , hythens or any special char , emojis